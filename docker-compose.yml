version: '3'

services:
  redis:
    image: redis:latest
    container_name: redis-server
    ports:
      - "6379:6379"
    environment:
      REDIS_PASSWORD: Redis@123
    command: ["redis-server", "--requirepass", "Redis@123"]
